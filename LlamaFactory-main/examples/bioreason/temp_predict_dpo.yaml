### 基础设置
# 这里用的是合并后的 SFT 字典作为底座！
model_name_or_path: models/Qwen2.5-VL-7B-BioReason-SFT
# 这里挂载你刚刚跑出来的 DPO 增量权重！(⚠️ 请把下面这行的 checkpoint-XXX 替换为你刚才查到的真实文件夹名)
adapter_name_or_path: saves/bioreason/dpo/checkpoint-final
stage: sft
do_predict: true
finetuning_type: lora
template: qwen2_vl

### 动态替换的参数 (存入新的 dpo 结果文件夹)
eval_dataset: test_CameraTrap_ohio-small-animals-balanced
output_dir: saves/bioreason/predict_results_dpo/test_CameraTrap_ohio-small-animals-balanced

### 预测参数
per_device_eval_batch_size: 1
predict_with_generate: true
max_new_tokens: 512
fp16: true
